{
  "name": "ct-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:create": "wrangler d1 create ct-analytics-db",
    "db:migrate:local": "wrangler d1 migrations apply ct-analytics-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply ct-analytics-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.105.1"
  }
}
